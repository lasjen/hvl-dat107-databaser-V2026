version: '3.8'

services:
  mongodb:
    image: mongodb/mongodb-community-server:7.0
    container_name: dat107-mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./mongodb-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=dat107_db